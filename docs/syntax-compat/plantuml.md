# PlantUML 语法兼容矩阵

> 每实现一项就更新一行。兼容目标：直接跑 PlantUML 官方文档与 PlantUML Server 上的示例。
> PlantUML Phase 4/Phase 5 矩阵内图型已收口；当前已落地各图型 streaming 子链路、专用块分发与 one-shot vs chunked 一致性校验。
> Streaming：默认 pipeline 已接入 `CachedTextMeasurer`、递归 `DrawText.measuredBounds`、`DrawCommandIndex` viewport culling 与 IR 结构化 entity key 的 `DrawCommandStore.updateEntities()`。

| 图类型              | 状态 | 关键语法                                                                                                                                                                                                                                                                                       | Phase                                                                                                                                | 备注                                                                                                                                                                                                                                                                                           | <br />                                                                                                                                                                                  | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| ---------------- | -- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----- | :------------------------------------------------------------------------------------------------------------------------------------- |
| sequence         | ✅  | participant/actor/control/boundary/database/collections/queue、`->`/`<-`/`->>`/`<<-`/`-->`/`<--`/`-->>`/`<<--`、常见复杂箭头装饰（`o->` / `->o` / `x->` / `->x` 等）、autonumber / `autonumber stop` / `autonumber resume`、activate/deactivate、return、note、`ref over`、`create` / `destroy`、`box ... end box`、group/alt/opt/loop/par/critical/break、`skinparam sequence/participant/actor/boundary/control/entity/database/collections/queue/note/box` 的基础样式映射 | 4 | 已接入 `Diagram.session(SourceLanguage.PLANTUML)`；`ref` 当前复用 note 语义承载，`box` 当前以 styleHints + 渲染层 cluster 背景实现，复杂箭头装饰通过 `StyleHints.extras` 下发渲染；`skinparam` 目前已支持 `BackgroundColor` / `BorderColor` / `FontColor` / `ArrowColor` / `FontSize` / `FontName` / `LineThickness` / `Shadowing`，并通过 `commonTest` 与 one-shot vs chunked 一致性校验 | <br /> | <br /> | <br /> | <br /> |
| usecase          | ✅  | actor / `actor/` / `:Actor:` / `:Actor:/`、`(usecase)` / `usecase X` / `usecase "Label" as X`、`-->` / `<--` / `..>` / `<..` / `.>` / `<.` / `--` / `..` / `<|--`、package / rectangle 嵌套、单行/多行 `note`、`include/extend` 标签归一化、常用 `skinparam actor/usecase/note/package/rectangle` 与 `ArrowColor` 样式映射 | 4 | 已接入 `Diagram.session(SourceLanguage.PLANTUML)`；business actor 变体会渲染 actor body slash；`skinparam actor/usecase/note/package/rectangle` 已支持 `BackgroundColor` / `BorderColor` / `FontColor` / `ArrowColor` / `FontSize` / `FontName` / `LineThickness` / `Shadowing`，并通过 `commonTest` 与 one-shot vs chunked 一致性校验 | <br /> | <br /> | <br /> | <br /> |
| class            | ✅  | class/interface/abstract/enum、别名 `class "Label" as X`、`package`、成员块、dotted member、可见性、泛型 `<T>`、单行/多行 note、\`<                                                                                                                                                                              | --`/`--                                                                                                                              | >`/`<                                                                                                                                                                                                                                                                                        | ..`/`..                                                                                                                                                                                 | >`/`\*--`/`o--`/`-->`/`<--`/`..>`/`<..`/`--`/`..\`                                                                                                                                                              | 4      | 已接入 `Diagram.session(SourceLanguage.PLANTUML)`；renderer 已区分 abstract/interface/enum 的头部样式，常用 `skinparam class/note/package` 的 `BackgroundColor` / `BorderColor` / `FontColor` / `ArrowColor` / `FontSize` / `FontName` / `LineThickness` / `Shadowing` 已可映射到节点/cluster/边与文本渲染，并通过 `commonTest` 与 one-shot vs chunked 一致性校验 |
| activity (新语法) | ✅ | `start` / `stop`、`:action;`、`if/else/elseif/endif`、`while/endwhile`、`repeat` / `repeat while (...)`、`fork` / `fork again` / `end fork`、swimlane `|name|`、单行/多行 `note`、legacy `(*)` / `(*top)` / quoted action / `-->` / `-right->` / `[label]` / `if "cond" then` / `"Action" as A1` / `A1 --> ...` / `=== label ===` / `===B1=== --> ...`、`partition Name #Color { ... }`、行内 `#Color:Action;`、常用 `skinparam activity` 与 `skinparam Activity*Color` 样式映射、关键字大小写不敏感（`IF` / `Repeat while` / `NOTE` / `PARTITION` 等与官方行为一致） | 4 | 已接入 `Diagram.session(SourceLanguage.PLANTUML)`；swimlane / partition 当前统一以内部 lane cluster 呈现，`repeat` 走 do-while 风格 lowering；同步条 `=== ... ===` 当前映射为 `ForkBar` 样式节点；`skinparam activity` 已支持 `BackgroundColor` / `BorderColor` / `FontColor` / `ArrowColor` / `FontSize` / `FontName` / `LineThickness` / `Shadowing`，并通过 `commonTest` 与 one-shot vs chunked 一致性校验 | <br /> | <br /> | <br /> | <br /> |
| component        | ✅  | component / `[Component] as X` / interface / `()` interface / port / portin / portout / database / queue、`-->` / `<--` / `..>` / `<..` / `--` / `..`、package / cloud / node / frame / rectangle 嵌套、单行/多行 `note`                                                                            | 4                                                                                                                                    | 已接入 `Diagram.session(SourceLanguage.PLANTUML)`；component 排版已拆为 primary graph 与 decoration layout 两段，`portin/portout`、anchored note 与 edge label 不再参与 Sugiyama 主图排层，而是在主业务节点稳定后统一测量、锚定、避让、重建边路由并重算 package/component cluster 包围盒；常用 `skinparam component/interface/port/database/queue/note/package/rectangle/frame/cloud/node` 的 `BackgroundColor` / `BorderColor` / `FontColor` / `ArrowColor` / `FontSize` / `FontName` / `LineThickness` / `Shadowing` 已可映射到节点/cluster/边与文本渲染，并通过 `commonTest` 与 one-shot vs chunked 一致性校验 | <br />                                                                                                                                                                                  | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| state            | ✅  | `state` / `state "desc" as X` / `state X : desc`、composite、nested composite、`[*]` / `[H]` / `[H*]`、`<<choice>>` / `<<fork>>` / `<<join>>` / `<<history>>` / `<<deep_history>>`、单行/多行 note、`-->`、方向声明、并行分区分隔线 `--` 的 region 建模、常用 `skinparam state/note` 与 `ArrowColor` 样式映射 | 4 | 已接入 `Diagram.session(SourceLanguage.PLANTUML)`；并行 region 当前以内部 synthetic composite 建模并渲染分隔线；`skinparam state/note` 已支持 `BackgroundColor` / `BorderColor` / `FontColor` / `ArrowColor` / `FontSize` / `FontName` / `LineThickness` / `Shadowing`，并通过 `commonTest` 与 one-shot vs chunked 一致性校验 | <br /> | <br /> | <br /> | <br /> |
| object           | ✅  | object / `object "Label" as X`、属性块 `{ ... }`、`Obj : key = value`、`-->` / `<--` / `..>` / `<..` / `--` / `..` / \`<                                                                                                                                                                         | --`/`\*--`/`o--`、单行/多行 note、package / namespace、`map`/`json\` 风格对象                                                                   | 4                                                                                                                                                                                                                                                                                            | 已接入 `Diagram.session(SourceLanguage.PLANTUML)`；note 采用 anchored note + dashed edge，package/namespace 复用 cluster 渲染，`map/json` 当前复用对象节点样式分型，常用 `skinparam object/map/json/note/package/namespace` 的 `BackgroundColor` / `BorderColor` / `FontColor` / `ArrowColor` / `FontSize` / `FontName` / `LineThickness` / `Shadowing` 已可映射到节点/cluster/边与文本渲染，并通过 `commonTest` 与 one-shot vs chunked 一致性校验 | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| deployment       | ✅  | node / artifact / database / cloud / frame / package / actor / queue / storage、`-->` / `<--` / `..>` / `<..` / `--` / `..`、嵌套、单行/多行 `note`                                                                                                                                                 | 4                                                                                                                                    | 已接入 `Diagram.session(SourceLanguage.PLANTUML)`；anchored note 会在布局后重定位，`actor/queue/storage` 已接入 `commonTest` 与 one-shot vs chunked 一致性校验，常用 `skinparam artifact/node/database/storage/queue/note/package/frame/cloud/actor` 的 `BackgroundColor` / `BorderColor` / `FontColor` / `ArrowColor` / `FontSize` / `FontName` / `LineThickness` / `Shadowing` 已可映射到节点/cluster/边与文本渲染 | <br />                                                                                                                                                                                  | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| erd              | ✅  | `entity`、属性块 `{ ... }`、`*`/`+`/`#` 属性标记、`<<PK/FK/UK>>`、crowfoot 关系装饰（如 `||--o{` / `}|..|{`）、关系 label、`note left/right/top/bottom of Entity : ...`、复杂别名（quoted label / alias）与非标准 attribute 语法（`name type`/`type name`） | 4 | 已接入 `Diagram.session(SourceLanguage.PLANTUML)`；crowfoot 操作符已结构化为左右端点并在渲染层绘制端点 marker，关系 label 仅显示用户文本；复杂别名、关系装饰与属性语法均通过 `commonTest` 与 one-shot vs chunked 一致性校验 |
| timing           | ✅  | `clock` / `binary` / `concise` / `robust` track declaration、`clock ... with period duration`、`clock ... with period duration duty N%` / `pulse duration` / `offset duration`、`"Label" as ID`、`hide time-axis`、`scale duration` / `scale duration as label`、`@time` / `@+duration` / `@time : label` marker、`ID is state` / `ID is state : text <<boundary>>` state segment、`A -> B : message`、`@10 <-> @50 : constraint`                                                                                                                                                                          | 5                                                                                                                                    | `PlantUmlTimingParser` 与 `PlantUmlTimeSeriesSubPipeline` 已完成主链路；普通 `@startuml ... @enduml` 中的 timing cue 会自动分发到 `TimeSeriesIR + GanttLayout` 时间轴渲染，`concise` track 以 compact 布局渲染为同一 lane 上的连续状态带与转场分隔线，并支持状态显示文本以及 `<<dashed>>` / `<<thick>>` / `<<none>>` / `<<solid>>` 边界样式，`binary` / `clock` track 渲染为高低电平 waveform；无显式 state 的 `clock ... with period ...` 会按周期自动生成 low/high 翻转段，`duty N%` / `pulse duration` 会生成非对称占空比 waveform，`offset duration` 可延迟 clock 首个 high 段，`robust` track 渲染为带浅底、粗边框、端点刻线和居中状态标签的专属状态胶囊，`scale` 驱动时间轴虚线网格与刻度标签，`hide time-axis` 可隐藏轴线网格，`@time : label` 渲染为事件标注，message 渲染为 track 间箭头，constraint 渲染为虚线区间标注，已接入 `commonTest` 与 one-shot vs chunked 一致性校验。                                                                                                      | <br />                                                                                                                                                                                  | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| wireframe (Salt) | ✅  | `@startsalt/@endsalt`、可选 `salt { ... }` wrapper、基础文本行、`[Button]`、`"placeholder"` / `Label: "placeholder"` / `Label: .` 输入框、`^Dropdown^` 下拉简写、`[X] Label` / `[ ] Label` checkbox、`(X) Label` / `( ) Label` radio、`--` / `==` / `..` separator、`<img:...>` / `img:...` / `{img ...}` 图片占位、`{* A | B | C }` 基础 tab row、`{* ... }` tab block、`{T ... }` tree、`{+ ... }` / `{# ... }` / `{frame ... }` frame、`{^ ... }` menu、`{! ... }` list、`{S ... }` scroll、pipe 行 table grid（如 `A | B | C`，含 cell 内控件与 `.` 空占位） | 5                                                                                                                                    | `PlantUmlSaltParser`、`WireframeIR + WireframeLayout` 与 `PlantUmlSaltSubPipeline` 已收口；控件统一降为 `WireBox.*`（button/input/image/tabbedGroup/plain），容器类 block 统一降为带 kind 前缀的 `WireBox.Plain`，布局阶段递归测量并生成稳定 path id，渲染层输出 container 边框/标题、tree/list/menu 连接线、scroll scrollbar、table grid 与 separator 线；已通过 `commonTest` 与 one-shot vs chunked 一致性校验。 | <br />                                                                                                                                                                                  | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| chart (pie/bar/line/scatter) | ✅ | `@startpie/@endpie`、`@startbar/@endbar`、`@startline/@endline`、`@startscatter/@endscatter`、`@startchart/@endchart`、普通 `@startuml` 中的 `pie` / `bar` / `line` / `scatter` / `chart` cue、`title`、`legend left/right/top/bottom` / `hide legend`、`skinparam pie/chart { BackgroundColor/BorderColor/FontColor/LineThickness/Shadowing }`、pie slice：`#Color; "Label" : value` / `Label : value #Color` / 百分比值、bar/line/scatter：`x-axis` / `y-axis` 与 `h-axis` / `v-axis`、数值轴范围、`spacing`、命名/着色 series（如 `line "Sales" [...] #3498db`）、line/scatter 坐标对 `[(x,y), ...]`、`Category : value` | 5 | `PlantUmlPieParser`、`PlantUmlXYChartParser` 与 chart 子流水线已收口；pie chart 降为共享 `PieIR + PieLayout`，bar/line/scatter chart 降为共享 `XYChartIR + XYChartLayout`，样式通过 `StyleHints.extras` 传递且不扩公共 IR 契约；布局阶段测量标题、坐标轴与图例/标签，渲染层输出 pie 扇区、bar 矩形、line 折线与点、scatter 点标记、坐标网格、图例与基础颜色/线宽/背景样式；已通过 `jvmTest` 定向 parser 与 one-shot vs chunked 集成校验。 | <br /> | <br /> | <br /> | <br /> |
| archimate        | ✅  | `archimate [type] #Color "Label" as ID <<stereotype>>`、`group "Layer" as L { ... }`、`A --> B : label`、`Rel(A, B, "label")` / `Rel_Composition` / `Rel_Aggregation` / `Rel_Assignment` / `Rel_Realization` / `Rel_Specialization` / `Rel_Access` / `Rel_Flow` / `Rel_Triggering` / `Rel_Influence` / `Rel_Serving`                                                                                                                                                                                              | 5                                                                                                                                    | `PlantUmlArchimateParser` 与 `PlantUmlArchimateSubPipeline` 已完成增强；元素会降到 `GraphIR` 并保留 ArchiMate element type / stereotype，渲染为业务、应用、技术、物理、动机、战略、实现迁移等分类图标节点，group 会降为 `GraphIR.Cluster` 并支持嵌套分层视图；关系会保留 `Rel_*` 类型语义并映射为 composition/aggregation 菱形端点、assignment/access 中点标记、realization/specialization 空心三角端点、flow/triggering/serving 等箭头与虚线/实线样式；已覆盖 composeApp archimate 样例，并通过 `commonTest` 与 one-shot vs chunked 一致性校验。                                                                      | <br />                                                                                                                                                                                  | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| c4 (C4-PlantUML) | ✅  | `!include` / `!includeurl` C4 import、`C4Context/C4Container/C4Component/C4Dynamic/C4Deployment`、`Person/System/Container/Component` 家族、`Deployment_Node`、`Boundary/*_Boundary` / `Node/Node_L/Node_R`、`Rel/BiRel/RelIndex/Rel_U/D/L/R/Back`、`BiRel_U/D/L/R`、`Lay_U/D/L/R`、`AddElementTag`、`AddRelTag`、`UpdateElementStyle`、`UpdateRelStyle`、`UpdateLayoutConfig`、`RoundedBoxShape` / `EightSidedShape`、`DashedLine` / `DottedLine` / `BoldLine`、`$tags`、`$link`、`SHOW_LEGEND()` / `LAYOUT_WITH_LEGEND()` | 5                                                                                                                                    | `PlantUmlC4Parser` 与 `PlantUmlC4SubPipeline` 已完成增强；C4 macro 行会降到 `GraphIR`，元素按 C4 类型渲染为 person/system/container/component/database/queue/deployment node 等样式，external 元素、technology、description、`$tags` 与 `$link` 均会保留；boundary、`Node/Node_L/Node_R` 与 block 形式 `Deployment_Node { ... }` 渲染为 cluster 并支持 tag 样式与 hyperlink；关系支持标签、技术、双向、方向端口、`Rel_Back` 虚线、`BiRel_*`、`Lay_*` 不可见布局边、关系 link、label textColor/offset；`AddElementTag` / `UpdateElementStyle` 支持填充、边框、文字颜色与 `RoundedBoxShape` / `EightSidedShape`，`AddRelTag` / `UpdateRelStyle` 支持线色、文字颜色、label 偏移、`DashedLine` / `DottedLine` / `BoldLine`；legend 宏会生成稳定 legend 节点并包含 tag legendText；已接入 `commonTest` 与 one-shot vs chunked 一致性校验。 | <br />                                                                                                                                                                                  | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| gantt            | ✅  | `@startgantt/@endgantt`、`Project starts`、section `-- Name --`、`[Task] starts/starts at/ends/ends at/lasts/happens at`、`[A] -> [B]` dependency、`[Task] on {Resource}`、`[Task] is colored in #Color/Name`、`[Task] is N% complete` / `is complete`、`[Task] is milestone/critical/dashed/bold`、`note bottom of [Task] : text`、`saturday/sunday are closed`、`YYYY-MM-DD is closed`、`YYYY-MM-DD to YYYY-MM-DD are closed`                                                                                 | 5                                                                                                                                    | `PlantUmlGanttParser` 与 `PlantUmlTimeSeriesSubPipeline` 已完成主链路；任务会降到 `TimeSeriesIR` 并复用 `GanttLayout` 渲染，支持 `@startgantt` 专用块、普通 `@startuml` 中 gantt cue 自动分发、section/resource 轨道、依赖顺序推导、任务颜色、progress overlay、milestone diamond、critical/dashed/bold 边框、任务 note、按星期关闭日与日期/日期范围 holiday band，并会在 `lasts N days` 遇到关闭日/holiday 时推导工作日结束时间；已接入 `commonTest` 与 one-shot vs chunked 一致性校验。                                                        | <br />                                                                                                                                                                                  | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| mindmap          | ✅  | `*`/`**`/`+`/`-` 层级、`*_` / `+_` / `-_` boxless、`+/-` 侧向继承、`: ... ;` 多行节点、轻量 Creole/Markdown 标记清洗（`**bold**` / `//italic//` / `__underline__` / `''mono''`）、stereotype（如 `<<vision>>`）、inline color（如 `[#orange]` / `[#112233]`）、前导 icon/emoji（如 `<&flag>`、`<:smile:>`、`<#green:sunny:>`）、tree style（`<style>` + `BackgroundColor` / `LineColor` / `FontColor` / `FontName` / `FontSize` / `FontStyle` / `LineThickness` / `RoundCorner` / `Shadowing` / `MaximumWidth` + `<<class>>` / `.myStyle *`）                 | 5                                                                                                                                    | `PlantUmlMindmapParser`、`@startmindmap` session 分发与 `TreeIR + MindmapLayout` 渲染链路已完成；当前已与 `wbs` 共享同一套 tree render 基础设施（颜色/标题行排版/节点 chrome/cubic edge），stereotype 会以独立标题行参与布局与渲染，inline color 已可作用到节点填充/描边/文字；前导 icon/emoji 已可作为节点前缀参与布局与渲染，其中 icon 会输出 `DrawIcon`，emoji 当前以 fallback 文本前缀呈现；boxless 节点跳过 chrome 并以着色文字呈现；`<style>` 中的 `.class { ... }` 与 `.class * { ... }` 已可通过 `<<class>>` 作用到节点填充、border、edge、标题/正文文字、字体、线宽、阴影、圆角与最大宽度；已接入 `commonTest` 与 one-shot vs chunked 一致性校验。 | <br />                                                                                                                                                                                  | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| wbs              | ✅ | org-mode `*` 层级、算术前缀 `+/-`、`<` / `>` 方向改写、`_` boxless、`: ... ;` 多行节点、inline color（如 `[#orange]` / `[#112233]`）、stereotype（如 `<<vision>>`）、前导 icon/emoji（如 `<&flag>`、`<:smile:>`、`<#green:sunny:>`）、普通 `@startuml` WBS cue、tree style（`<style>` + `BackgroundColor` / `LineColor` / `FontColor` / `FontName` / `FontSize` / `FontStyle` / `LineThickness` / `RoundCorner` / `Shadowing` / `MaximumWidth` + `<<class>>` / `.myStyle *`） | 5 | `PlantUmlWbsParser`、`@startwbs` / 普通 `@startuml` session 分发与 `TreeIR + MindmapLayout` 渲染链路已完成；当前已与 `mindmap` 共享同一套 tree render 基础设施（颜色/标题行排版/节点 chrome/cubic edge），inline color 已可作用到节点填充/描边/文字（boxless 节点以着色文字呈现），stereotype 会以独立标题行参与布局与渲染；前导 icon/emoji 已可作为节点前缀参与布局与渲染，其中 icon 会输出 `DrawIcon`，emoji 当前以 fallback 文本前缀呈现；`<style>` 中的 `.class { ... }` 与 `.class * { ... }` 已可通过 `<<class>>` 作用到节点填充、border、edge、标题/正文文字、字体族/字号/粗斜体、线宽、圆角、阴影与最大宽度，布局阶段会按 `FontSize` / `MaximumWidth` 测量，且 multiline 结束行上的 `; <<class>>` 也可识别；已通过 `jvmTest` parser 与 one-shot vs chunked 集成校验。 | <br /> | <br /> | <br /> | <br /> |
| network (nwdiag) | ✅  | `nwdiag { ... }`、`network name { ... }`、`inet { ... }` / `inet name { ... }`、`group name { ... }`、`address = ...`、节点声明、节点属性（`address` / `description` / `label` / `shape` / `color` / `fill` / `bgcolor` / `bordercolor` / `textcolor`）、显式连接（`A -- B` / `A -> B` / `A <-> B : label`）、跨 network 同名节点连接                                                                                                                                                                              | 5                                                                                                                                    | `PlantUmlNetworkParser`、`PlantUmlNetworkSubPipeline` 与 `GraphIR + Sugiyama` 渲染链路已完成增强；当前支持 `@startuml` 内 `nwdiag {}` 与 `@startnwdiag/@endnwdiag` 专用块，network/inet 会渲染为 cluster，network 内 group 会降为 nested cluster；节点会保留 address/description/label/shape/color 等属性，并将 cloud/inet/database/queue/actor/component/box 等 shape 映射为对应视觉，显式连接支持无向/单向/双向箭头与 label，跨网络同名节点以虚线连接；已通过 `commonTest` 与 one-shot vs chunked 一致性校验。                                                                            | <br />                                                                                                                                                                                  | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| ditaa            | ✅  | `@startditaa/@endditaa`、`+---+` / `|...|` 与 `/---\` / `\---/` ASCII box 识别、ditaa 形状标记（`{d}` / `{s}` / `{io}` / `{c}` / `{o}` / `{mo}` / `{tr}`）、水平/垂直连接、无向/单向/反向/双向箭头、`=` 粗线、`:` 虚线、box 内颜色标记（如 `cRED` / `cGRE` / `cBLU` / `cYEL` / `cF80`）、`skinparam handwritten true` | 5                                                                                                                                    | `PlantUmlDitaaParser` 与 `PlantUmlDitaaSubPipeline` 已完成增强；当前 ASCII box 会降到 `GraphIR` 节点，并按原始网格坐标进行稳定布局，box 内颜色标记会从可见标签中移除并映射到 `NodeStyle.fill`，`{d}` / `{s}` / `{io}` / `{c}` / `{o}` / `{mo}` / `{tr}` 会映射为 note/database/parallelogram/diamond/ellipse/hexagon/trapezoid 等节点形状，圆角 corner 字符会映射为更大的渲染圆角；连接线支持水平/垂直、无向/单向/反向/双向、粗线与虚线语义，渲染层会按节点相对位置选择左右/上下端点进行正交路由；`skinparam handwritten true` 会启用基于 ID/hash 的确定性 jitter 手绘边框与连线；已接入 `commonTest` 与 one-shot vs chunked 一致性校验。计划中曾将 ditaa 归入 `StructIR`，但本轮未扩公共 IR 契约，继续以 `GraphIR` 表达 box/edge。 | <br />                                                                                                                                                                                  | <br />                                                                                                                                                                                                          | <br /> | <br />                                                                                                                                 |
| json / yaml      | ✅ | `@startjson` / `@startyaml` 专用块、JSON object / array / string / number / boolean / null、JSON escape 与 unicode escape、严格 JSON literal 校验、YAML key-value / nested object / list / inline list、inline object `{...}`、quoted `#` 保真、document marker `---` / `...`、boolean/null 规范化、literal block scalar `|` / `|-` / `|+`、folded block scalar `>` / `>-` / `>+`、容器折叠/展开 affordance | 5 | `PlantUmlStructParser`、`StructIR + StructLayout` 与 `PlantUmlStructSubPipeline` 已完成；JSON/YAML 会渲染为路径稳定的嵌套结构行，container 路径与 scalar 类型通过 `StyleHints.extras` 传递，布局/渲染层输出 `[-]` 折叠/展开提示并对 number/boolean/null 使用类型化文字颜色；YAML 分发保留缩进传递给结构 parser，`|` 系列会保留换行降为 scalar，`>` 系列会折叠为空格串 scalar，quoted comment 与 inline object/list 均已支持；已通过 `jvmTest` parser 与 one-shot vs chunked 集成校验。 | <br /> | <br /> | <br /> | <br /> |

## 公共

- 本矩阵内专用块已识别：`@startuml ... @enduml`、`@startmindmap ... @endmindmap`、`@startwbs ... @endwbs`、`@startjson ... @endjson`、`@startyaml ... @endyaml`、`@startnwdiag ... @endnwdiag`、`@startgantt ... @endgantt`、`@startsalt ... @endsalt`、`@startditaa ... @endditaa`、`@startpie ... @endpie`、`@startbar ... @endbar`、`@startline ... @endline`、`@startscatter ... @endscatter` 与 `@startchart ... @endchart`。
- `skinparam` 当前已在 `sequence`、`activity`、`usecase`、`state`、`class`、`component`、`deployment`、`object`、`pie/bar/line/scatter/chart` 与 `ditaa handwritten` 子链路支持对应基础样式；`mindmap` / `wbs` 使用 `<style>` tree style，其他 Phase 5 图型优先通过各自语法属性、tag 或 payload 样式承载，矩阵外 PlantUML 图型按 `PLANTUML-W001` 诊断处理。
- 黄金语料：`composeApp/src/commonTest/resources/plantuml/<diagram>/`
